# Reddit API credentials (OAuth client credentials flow)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=forklore.ai/0.1.0

# Vercel KV (legacy - can remove if not using)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Upstash Redis (for rate limiting)
# Get free tier at: https://console.upstash.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Database (Neon Postgres with PostGIS + pg_trgm)
DATABASE_URL=
